# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DontSleepNow' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DontSleepNow
pod 'Firebase/Core'
  target 'DontSleepNowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DontSleepNowUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'DontSleepNow WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DontSleepNow WatchKit App

end

target 'DontSleepNow WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DontSleepNow WatchKit Extension

end
